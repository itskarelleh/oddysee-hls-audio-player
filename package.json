{
  "name": "oddysee-typescript-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "test": "npm run test --workspaces",
    "fixture:generate": "bash fixture/generate-hls.sh",
    "fixture:serve": "node fixture/dev-server.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
